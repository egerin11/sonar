#include"Header.h"
#include<stdio.h>

int main()
{
	
	int mas[N][M];
	int col=0, row=0;
    fillArray(mas, row);
	printArray(mas, row);
	product_of_numbers(mas, row);
	max_sum(mas, row);
	

	return 0;
 }